import { CategoryMapperPipe } from './category-mapper.pipe';

describe('CategoryMapperPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryMapperPipe();
    expect(pipe).toBeTruthy();
  });
});
